server:
  port: 8888

#actuator
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,env,prometheus

spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MrKami-Kirill/config-repo
          default-label: main
          search-paths: '{application}'
          username: mrkami19940@gmail.com
          password: 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
        encrypt:
          enabled: false
        default-profile: default